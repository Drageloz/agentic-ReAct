# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
starlette==0.41.3

# LLM providers
openai>=1.58.1
anthropic==0.42.0
# LangChain — satisfies prueba técnica requirement; LangChainAdapter implements LLMPort
langchain-openai>=0.3.0
langchain-core>=0.3.0
# Vector store real con metadata filtering — ChromaRAGAdapter
langchain-community>=0.4.0
chromadb>=1.0.0

# Database — SQL Server (local Docker + Azure SQL)
sqlalchemy==2.0.36
aioodbc==0.5.0          # async ODBC driver for SQLAlchemy (mssql+aioodbc)
pyodbc==5.2.0           # ODBC driver dependency

# Configuration
pydantic==2.10.3
pydantic-settings==2.10.1

# Utilities
python-dotenv==1.0.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
pytest-mock==3.14.0
greenlet==3.3.2

